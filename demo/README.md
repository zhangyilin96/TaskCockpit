# 安全 Demo 资料

本目录只包含虚构示例，不包含任何真实项目、私人记忆、账号信息或 AI 对话。

- `sample-bootstrap.json`：从旧 AI 对话导入次级项目时的示例返回
- `sample-status-review.json`：现状总结弹窗的示例返回
- `sample-backup.json`：只包含虚构内容的独立 Demo Workspace 备份

推荐先在应用中进入 **演示模式**，再使用这些资料。Demo Workspace 与正式 Workspace 完全隔离。
